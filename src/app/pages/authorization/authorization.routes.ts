import { Routes } from '@angular/router';
import { AuthorizationComponent } from './container/authorization/authorization.component';

export const authorizationRoutes: Routes = [
    {
        path: '',
        component: AuthorizationComponent,
    },
];
